# from wtforms import Form, BooleanField, TextField, PasswordField, validators
#
# class RegistrationForm(Form):
#     entityname = TextField('Input the name of your Entity', [validators.Length(min=4, max=25)])
#     field1 = TextField('Input the first field of your Entity', [validators.Length(min=4, max=25)])
#     field2 = TextField('Input the second field of your Entity', [validators.Length(min=6, max=35)])
    # password = PasswordField('New Password', [
    #     validators.Required(),
    #     validators.EqualTo('confirm', message='Passwords must match')
    # ])
    # confirm = PasswordField('Repeat Password')
    # accept_tos = BooleanField('I accept the TOS', [validators.Required()])
# class EntityForm(form):
